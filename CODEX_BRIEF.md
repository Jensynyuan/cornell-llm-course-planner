# Codex Brief — Cornell LL.M. Planner v5.33

## 产品目标

维护一个首次访问默认英文、可切换中文、离线可用的美国法学院 LL.M. 选课规划器。Cornell 是默认且完整预装的数据集；其他学校通过适配器导入。

## 不可破坏的规则

1. Cornell 数据必须在构建阶段完成抓取与机器翻译，最终用户不得被要求配置 OpenAI API。
2. `data/cornell.catalog.zh-CN.js` 必须可以离线直接加载；不要把启动流程改成联网拉取。
3. 课程目录与学期班次要分开：没有班次时间的课程可搜索，但不可伪造课表色块。
4. 每门课程必须显示一个 NY Bar 状态和一个选课权限状态。
5. 所有标签使用统一高度、字号、圆角和内边距。
6. 校区不是教室。只有 meeting-level 楼宇／房间字段才能显示为具体地点。
7. IALS 仅在 Orientation 实际日期内出现。
8. 拖拽课程卡后，按照所选班次官方日期和时间自动落位；时间重叠必须弹出明确冲突提示。
9. 课程推荐不得写死某个用户的履历；使用职业方向和课程偏好的组合式评分。
10. 外校导入的 NY Bar 状态默认 `review`，除非有该校官方依据。
11. Cornell 用户可见中文内容必须以官方英文原文的忠实翻译为先；正式发布库的每门课程都必须有中文课程名与中文课程说明。官方当前及历史来源均未发布简介时，中文字段应明确说明“官方未发布”，不得编造课程内容，也不得保留“待翻译”占位符。
12. 每次发布必须运行当前版本的发布校验；v5.33 至少使用 `node scripts/verify_release_v5_33.mjs`、`node scripts/simulate_three_students_v5_31.mjs` 与本地浏览器硬门。课程数、双语 offering ID、四个浏览器目录全局变量、Spring NY Bar 映射、同号跨学期隔离、默认英文、双语公告、法学院校历、日历集成及真实生产 NY Bar 判定器均须通过。
13. Cornell 同时列入多个 NY Bar 类别的课程必须只分配一次。默认归类可依据整个秋春学年其他已选课程选择增益最大的官方类别，但用户必须能手动调整；任何情况下不得重复计入两个分类进度。
14. 从日历导入的个人事件必须只保存在当前浏览器，不得上传；个人事件必须参与课程冲突检查，导出课程日历必须按真实上课日期生成。
15. 作者公告、版本记录及日历操作必须同时维护英文和中文文案；新访问者默认英文，已保存的语言偏好继续生效。
16. Fall 与 Spring 数据必须同时在 `app.js` 前加载；课程与语言版本一律按 offering ID 配对，禁止按课程号合并，以免春秋同号课程串库。
17. 课程检索默认可见两个学期；学期与各 facet 支持多选，同一 facet 内采用 OR、不同 facet 间采用 AND。学期选择只过滤课程库和当学期推荐，不得过滤或删除个人已选课程。NY Bar 24 学分及分类按 Fall + Spring 整个学年累计；LL.M. 10–15 注册学分门槛必须按 Fall、Spring 两个学期分别显示和判断。
18. Cornell Spring 日历必须以 Cornell Law 的 JD／Ithaca LL.M. 专门校历为准，不得用 University 普通校历代替；假期名称要在周课表中可见，特殊换课日必须按官方 weekday pattern 渲染。
19. 发布浏览器硬门必须覆盖：选中 Fall 课程后切至 Spring，年度进度和本地选择不变；再加入 Spring 课程后两学期注册学分分别更新；中英文 allocation 标签全部位于课程卡边界内；Spring 假期周无课程块且显示准确名称。
20. 每次从其他页面进入课表必须按浏览器本地日期显示所在周，并保留 Fall 与 Spring 第一周快捷入口；无课日和周次必须按显示日期所属学期判断。
21. 发布前必须跑“学期 × 上课形式”组合矩阵，并把运行数据与页面结果逐项比较；至少对 Spring 的讲授课、研讨课、诊所、实践课与独立研究设非零断言。只检查筛选控件存在，不构成通过。
22. Spring 中英文 JSON 与浏览器 JS 必须分别深度一致：中文全局为 `CORNELL_SPRING_2027_COURSE_CATALOG`，英文全局为 `CORNELL_SPRING_2027_COURSE_CATALOG_EN`；任一缺失、覆盖或串库都必须阻断发布。
23. 多分类 NY Bar 课程的用户明确归类必须写入本地状态，即使所选值等于数据中的默认分类；切换后进度条、课程列表、推荐、课表与详情必须在不刷新页面的情况下同步更新，并在刷新后保留。
24. Cornell 的标准 Lecture、Seminar 或 Discussion 班次不得仅因 instruction mode 留空或显示 `Not published` 而被扣除 NY Bar 课堂学分；只有明确标为 Online／Remote 的班次才因授课方式排除。原始课程或 meeting 地点写明 Online／Remote 时同样必须排除。独立研究仍不计入，Clinic、Practicum、混合授课或项目限制班次仍须确认。用户手工填写或本地同步地点不能改变 NY Bar 资格。判定规则必须由 `nybar-eligibility.js` 的纯函数统一提供，测试须直接执行该生产函数。进度面板必须提供逐门计算明细，任何待确认或排除学分都须自动展开并说明原因。

## Cornell 更新工作流

1. 从 Cornell 官方 Catalog 和 Class Roster/API 获取原始数据。
2. 合并课程目录与当前学期 section/meeting 元数据。
3. 在构建机器上完成批量机器翻译并应用锁定法律术语词表；不要在浏览器端翻译。
4. 输出：
   - `data/cornell-law-YYYY-YY.zh-CN.json`
   - `data/cornell.catalog.zh-CN.js`
   - `data/cornell-law-spring-YYYY.en.json` 与 `.zh-CN.json`
   - `data/cornell.catalog.spring-YYYY.en.js` 与 `.zh-CN.js`
5. 执行完整性检查：课程数、中文标题与说明全覆盖、官方无简介状态、班次日期、时间格式、结构化上课形式、重复 ID、NY Bar 标签、英文原文字段和锁定术语检查。
6. 启动本地服务器，以 1600×1000 视口检查课程检索、中文课程详情、课表、学期切换与窄屏布局；逐项点击 Spring 及所有上课形式筛选，不能只做静态代码检查。
7. 随机模拟三位学生选课，检查学分范围、时间冲突、重复 offering ID、排除课程、跨学期隔离与 NY Bar 课程；全部自动化和浏览器检查通过后才可发布。

## 其他法学院适配

按照 `T14_IMPORT_GUIDE.md` 和 `IMPORT_SCHEMA.md` 创建独立数据文件。不要假定 T14 存在统一 API，也不要绕过学校权限。

## 交互与教室规则

- 课程卡必须支持原生鼠标拖拽到左侧“我的课表”、已选课程区和周课表；不要只支持点击按钮。
- 多班次先弹出班次选择，冲突必须弹窗，不得静默覆盖。
- 教室同步使用 `/api/cornell/sync-rooms`，按班号从 Fall 2026 官方课表读取；不得用校区或院系名称代替房间。
- 用户页面不得出现“对开发者”“读取用户简历”“构建阶段”等内部实现式文案。
- 保留主页面署名：本页面由袁敬轩使用 Codex 设计。

## v5.2 翻译发布规则

- 使用 `scripts/build_v5_2_release.mjs` 将已批准的中文源数据构建为 JSON 和浏览器目录；不要手工修改两份运行数据中的单门课程。
- `data/qa/` 中的三份 JSON 是本次发布的术语及翻译质检记录；后续替换课程数据库时须同时产生新的检查记录。
- 允许保留课程号、教师姓名、缩写、班号及官方链接；英文课程正文只应在官方页面中查看，不应出现在本地中文详情中。
